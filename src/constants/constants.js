const LOG_TYPES = {
    ERROR: "error",
    AUDIT: "audit",
};

const HTTP_CODES = {
    OK: 200,
    BAD_REQUEST: 400,
    INTERNAL_SERVER_ERROR: 500,
};

module.exports = { LOG_TYPES, HTTP_CODES };
